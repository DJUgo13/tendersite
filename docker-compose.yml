services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: tendersite
      MYSQL_USER: tendersite
      MYSQL_PASSWORD: tendersite
      MYSQL_ROOT_PASSWORD: password
      MYSQL_ROOT_HOST: "%"
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"

volumes:
  mysql_data:
